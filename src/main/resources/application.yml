# MYSQL
spring:
  datasource:
    url: jdbc:mysql://balance-database.cfbqz6prbj9w.ap-northeast-2.rds.amazonaws.com:3306/balance-database
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: Likelion_admin
    password: Likelion_project
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        show_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
