# application
spring:
  profiles:
    active: local
---

spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    url: jdbc:mysql://likelion.cfbqz6prbj9w.ap-northeast-2.rds.amazonaws.com/mysql
    username: admin
    password: LikeLion
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

#spring:
#  h2:
#    console:
#      enabled: true
#  datasource:
#    url: jdbc:h2:~/balance-db2
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create

---

spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:mysql://localhost:3306/goldbalance_schema?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: goldbalance-user
    password: goldbalance1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        physical_naming_strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
