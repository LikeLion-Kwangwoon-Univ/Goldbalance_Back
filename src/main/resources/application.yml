# MYSQL
spring:
  datasource:
    url: jdbc:mysql://likelion.cfbqz6prbj9w.ap-northeast-2.rds.amazonaws.com:3306/mysql
    username: admin
    password: LikeLion
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
